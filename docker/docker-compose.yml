services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.7.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.7.0
    container_name: kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      # Two listeners: PLAINTEXT for host access (sbt run), INTERNAL for container-to-container
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,INTERNAL:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,INTERNAL://0.0.0.0:29092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,INTERNAL://kafka:29092
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      # Convenience for local dev: auto-create checkout.events on first use. The stricter
      # alternative is explicit topic administration on startup.
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      # Short retention for local dev (1 hour)
      KAFKA_LOG_RETENTION_MS: 3600000
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0

  # ---------------------------------------------------------------------------
  # The five services are run from sbt during development. Containerizing them
  # (one Dockerfile per assembled fat-jar, wired to the INTERNAL listener at
  # kafka:29092) is a later step. Sketch:
  #
  # order-service:
  #   build: { context: .., dockerfile: docker/order.Dockerfile }
  #   depends_on: [kafka, inventory-service, payment-service]
  #   ports: ["8080:8080"]
  #   environment:
  #     KAFKA_BOOTSTRAP_SERVERS: kafka:29092
  #     INVENTORY_BASE_URI: http://inventory-service:8081
  #     PAYMENT_BASE_URI:   http://payment-service:8082
  # inventory-service: { ... ports: ["8081:8081"] }
  # payment-service:   { ... ports: ["8082:8082"] }
  # notification-service: { depends_on: [kafka], environment: { KAFKA_BOOTSTRAP_SERVERS: kafka:29092 } }
  # audit-log-service:    { depends_on: [kafka], environment: { KAFKA_BOOTSTRAP_SERVERS: kafka:29092 } }
  # ---------------------------------------------------------------------------
